---
streams:
    description:
        A dummy environment used for testing the streams layer.

    layers:
          - layer: streams
            layer_type: streams
            streams:
              - name: InputStream
                shard_count: 1
              - name: OutputStream
                shard_count: 1
