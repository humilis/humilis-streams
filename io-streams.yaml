---
io-streams-test:
    description:
        A dummy environment used for testing the io-streams layer.
    tags:
        "cost:chapter": "data"
        "cost:squad": "product"
        "cost:project": "nfh-event-tracker"
    layers:
        # The storage layer
        - {layer: storage}
        - {layer: firehose-rsc}
        - {layer: io-streams}
